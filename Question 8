public class Main {

    public static boolean AllPositive(int[] arr, int start, int end) {

        // Base case
        if (start == end && arr[start]>0) {
            return true;
        }
          if (start == end && arr[start]<0) {
            return false;
        }

        int mid = (start + end) / 2;

        // Recursive calls
       boolean x= AllPositive(arr, start, mid);
       boolean y =  AllPositive(arr, mid + 1, end);
       
       if (x &&y ==true)
       {
         return true;
         }

         return false;
         

    }

    public static void main(String[] args) {

        int[] arr = {3, 7, -5, 1, 8};

        boolean result = AllPositive(arr, 0, arr.length - 1);

        System.out.println("Maximum = " + result);
    }
}
